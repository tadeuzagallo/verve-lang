enum Bool {
  True
  False
}

True
False

enum Nat {
  Zero
  Succ(Nat)
}

Zero
Succ(Zero)
Succ(Succ(Zero))

// newline rules

enum A { B }

// enum A { B C } - fails: multiple constructors must be separated by a newline

enum X {
  A
  B
}
