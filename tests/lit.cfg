# vi: ft=python

import os
import lit.formats

verve = os.path.join(os.path.dirname(__file__), '../verve')

config.environment['CAML_LD_LIBRARY_PATH'] = os.environ['CAML_LD_LIBRARY_PATH']

config.name = 'Verve'
config.suffixes = ['.vrv']
config.test_format = lit.formats.ShTest(True)

config.substitutions.append(('%check', 'OutputCheck --comment=".*//" %s'))
config.substitutions.append(('%verve', '{} %s 2>&1'.format(verve)))
config.substitutions.append(('%not', 'eval !'))
