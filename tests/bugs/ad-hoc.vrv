// RUN: %not %verve
interface Intish<T> {
  fn to_int(T) -> Int
}

implementation Intish<Int> {
  fn to_int(x: Int) -> Int { x }
}

fn f<T: Intish>(f: (T) -> Int, x: T) -> Int {
  f(x)
}
f(to_int, 4)
