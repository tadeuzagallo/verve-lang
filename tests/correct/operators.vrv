// RUN: %verve | %check
operator (x: Int) + (y: Int) -> Int {
  int_add(x, y)
}

3 + 5 // 8 : Int

interface Num<T> {
  operator (T) + (T) -> T
}

implementation Num<Int> {
  operator (x: Int) + (y: Int) -> Int {
    int_add(x, y)
  }
}

5 + 37 // CHECK: 42 : Int
