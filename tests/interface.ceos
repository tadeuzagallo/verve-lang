interface Show<T> {
  virtual show(T) -> String
}

implementation Show<String> {
  fn show(s) {
    "SHOW STRING"
  }
}

implementation Show<Int> {
  fn show(n) {
    "SHOW INT"
  }
}

print(show(""))
print(show(0))
